# Boards

## Get All Boards

> The above command returns JSON structured like this:

<%= partial "includes/json/boards.md.erb" %>

This endpoint retrieves all boards.

### HTTP Request

`GET /boards`

### Available Parameters

Parameter | Description
--------- | -----------
all | Show all boards. By default, only boards for rating are show

### Expand Parameters

Parameter | Description
--------- | -----------
roles | [Board roles](#roles)
owner | Filter owner information
platform | Platform model

## Get Board

> The above command returns JSON structured like this:

<%= partial "includes/json/board.md.erb" %>

This endpoint retrieves a specific board.

### HTTP Request

`GET /boards/<ID>`

### Expand Parameters

Parameter | Description
--------- | -----------
roles | [Board roles](#roles)
owner | Filter owner information
platform | Platform model

## Board Updating

> The above command returns JSON structured like this:

<%= partial "includes/json/board.md.erb" %>

This endpoint updates an exist board.

### HTTP Request

`PUT /boards/<ID>` 

### Available Parameters

Parameter | Description
--------- | -----------
is_rating_list | Use this board for ratings
top_priority | Number of highlighted tasks in the top
votes_lifetime | Lifetime of votes in days
notifications | Settings for the Slack schedule field

## Board Issues

> The above command returns JSON structured like this:

<%= partial "includes/json/issues.md.erb" %>

This endpoint retrieves a list of issues in a specific board.

### HTTP Request

`GET /boards/<ID>/issues`

## Create Vote

This endpoint creates a new vote into the board

### HTTP Request

`POST /boards/<ID>/vote` 

### Available Parameters

Parameter | Description
--------- | -----------
issue_id | An issue ID
rating_id | An issue rating ID
vote | Value of vote

## Users Votes

> The above command returns JSON structured like this:

<%= partial "includes/json/users-votes.md.erb" %>

Returns a list of votes divided by users 

### HTTP Request

`GET /boards/<ID>/users-votes` 

### Available Parameters

Parameter | Description
--------- | -----------
platform_user_id | A platform user ID (optional)
role_id | A role ID (optional)
only    | (voted/unvoted) Return only voted or unvoted issues

## Summary Votes

> The above command returns JSON structured like this:

<%= partial "includes/json/summary-votes.md.erb" %>

This endpoint returns a list of votes divided by users.
If `rating_id` in `votes` collection is null – it means that this is a summary vote 

### HTTP Request

`GET /boards/<ID>/summary-votes`

### Available Parameters

Parameter | Description
--------- | -----------
assignee_id | ID of user for filtration

## Unvoted issues count

> The above command returns JSON structured like this:

```json
{
    "data": 5375
}
```

This endpoint returns a number of unvoted issues in the board, for a current user only 

### HTTP Request

`GET /boards/<ID>/unvoted` 

## Platform users on the board

> The above command returns JSON structured like this:

<%= partial "includes/json/board-users.md.erb" %>

Returns a list of platform users with active issues on the board 

### HTTP Request

`GET /boards/<ID>/platform-users`

### Expand Parameters

Parameter | Description
--------- | -----------
user | [User model](#get-user)
